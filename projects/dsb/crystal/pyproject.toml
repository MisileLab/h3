[project]
name = "crystal"
version = "0.1.0"
description = "LLM that do things"
readme = "README.md"
requires-python = "==3.13.2"
dependencies = [
  "pydantic-ai-slim[duckduckgo,logfire,vertexai]==0.1.4",
  "rich==14.0.0",
]
